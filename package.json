{
  "name": "project-jellyfish",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "./web/*"
  ],
  "scripts": {
    "dev": "concurrently \"go run main.go\" \" cd web && npm run client:dev\"",
    "client:dev": "cd web && vite",
    "server:dev": "go run main.go",
    "prepare": "cd .. && husky install",
    "start": "go run main.go"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "concurrently": "^7.1.0"
  }
}